import * as React from 'react';
import { Link, BrowserRouter as Router, useHistory } from 'react-router-dom';

const getPicture2: React.FC = ({history}: any) => {
	// const history = useHistory();
    return (
        <div>
            <h2>
							getPicture2
            </h2>
        </div>
    );
};

export default getPicture2;