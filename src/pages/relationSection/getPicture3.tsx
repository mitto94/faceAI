import * as React from 'react';
import { Link, BrowserRouter as Router, useHistory } from 'react-router-dom';

const getPicture3: React.FC = ({history}: any) => {
	// const history = useHistory();
    return (
        <div>
            <h2>
							inpuRela
            </h2>
        </div>
    );
};

export default getPicture3;