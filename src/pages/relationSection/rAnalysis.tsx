import * as React from 'react';
import { Link, BrowserRouter as Router, useHistory } from 'react-router-dom';

const rAnalysis: React.FC = ({history}: any) => {
	// const history = useHistory();
    return (
        <div>
            <h2>
						rAnalysis
            </h2>
        </div>
    );
};

export default rAnalysis;